@model Device

<div class="col-sm-4 col-lg-4 col-md-4">
    <div class="thumbnail">
        <img src="@Model.ImageThumbnailUrl" alt="">
        <div class="caption">
            <h3 class="pull-right">@Model.DeviceId.ToString("c")</h3>
            <h3>
                <a asp-controller="Device" asp-action="Details" asp-route-id="@Model.DeviceId">@Model.Name</a>
            </h3>
            <p>@Model.ShortDescription</p>
        </div>
        <div class="addToCart">
            <p class="button">
                <a class="btn btn-primary"
                   asp-controller="ShoppingCart"
                   asp-action="AddToShoppingCart"
                   asp-route-deviceId="@Model.DeviceId">Add to cart</a>
            </p>
        </div>
    </div>
</div>